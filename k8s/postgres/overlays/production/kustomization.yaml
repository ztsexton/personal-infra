apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Base, unmodified. Production keeps the resource requests it has always had;
# this overlay exists only so staging has somewhere to differ from it.
resources:
  - ../../base
