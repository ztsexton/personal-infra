apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: petfoodfinder-staging-cert
  namespace: web
spec:
  secretName: petfoodfinder-staging-tls
  issuerRef:
    name: cloudflare-dns
    kind: ClusterIssuer
  dnsNames:
    - staging.petfoodfinder.app
