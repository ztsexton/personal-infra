apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: vigilo-ingress
  namespace: web
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - vigilo-staging.zachsexton.com
    secretName: zachsexton-wildcard-tls
  rules:
  - host: vigilo-staging.zachsexton.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: vigilo
            port:
              number: 80
