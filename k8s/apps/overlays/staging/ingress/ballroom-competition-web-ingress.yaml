apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ballroom-competition-web-ingress
  namespace: web
spec:
  ingressClassName: traefik
  tls:
    - secretName: petfoodfinder-staging-tls
      hosts:
        - staging.petfoodfinder.app
  rules:
    - host: staging.petfoodfinder.app
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ballroom-competition-web
                port:
                  number: 80
