apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spotifybutler-ingress
  namespace: web
spec:
  ingressClassName: traefik
  tls:
  - hosts:
    - spotifybutler-staging.zachsexton.com
    secretName: zachsexton-wildcard-tls
  rules:
  - host: spotifybutler-staging.zachsexton.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: spotifybutler
            port:
              number: 80
