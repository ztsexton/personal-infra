apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: spotifybutler
  namespace: web
spec:
  ingressClassName: traefik
  tls:
    - hosts:
        - zachsexton.com
      secretName: zachsexton-com-tls
  rules:
    - host: zachsexton.com
      http:
        paths:
          - path: /spotifybutler
            pathType: Prefix
            backend:
              service:
                name: spotifybutler
                port:
                  number: 80
